## YC Fitness API

- API docs: `http://localhost:<PORT>/docs` (Swagger UI) and `http://localhost:<PORT>/docs.json` (OpenAPI JSON).

### Setup
- Install deps: `npm install`
- Copy env: `cp env.example .env` (fill values)
- Run dev: `npm run start:dev`
- Build+start: `npm run start`

